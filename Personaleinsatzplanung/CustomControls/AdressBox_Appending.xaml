<UserControl x:Class="Personaleinsatzplanung.CustomControls.AdressBox_Appending"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Personaleinsatzplanung.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:AdressBox LabelWidth="{Binding Path=LabelWidth, RelativeSource={RelativeSource FindAncestor, AncestorType=local:AdressBox_Appending, AncestorLevel=1}}"/>
        <Button Grid.Row="1" Margin="0,5,0,0">+</Button>
    </Grid>
</UserControl>
